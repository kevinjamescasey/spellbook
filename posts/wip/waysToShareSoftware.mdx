
download binary 
download installer that runs and installs more
download scripts and interpreter seperately
OS specific installer that downloads files and places them 
runtime multi-version installer that can run different version in different shells
programming lanuage specific code installer (dependency management)
programming VM specific bytecode installer (depenency management with language interop)
download images to run in containers
download images to run as VMs
app store (steam, epic, Android, ios, Nintendo store)
OS updates
provide web API to call and invoke software in central location
provide web UI that fetches and runs code in a browser


download can happen through web browser, ftp, etc
archive, bundle, image
running
    launch from desktop manager, shell, app store
    include in other program
    


| method | how it runs |  |
